%p#notice= notice

= render partial: 'partials/search_bar'

%h1 Listing Bands
%table
  %thead
    %tr
      %th{colspan: '3'}
  %tbody
    - @bands.each do |band|
      %tr
        %td= band.name
        %td= link_to 'Show', band
