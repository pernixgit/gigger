%p#notice= notice

= render partial: 'partials/search_bar'

%h1 Listing Events
%table
  %thead
    %tr
      %th{colspan: '3'}
  %tbody
    - @events.each do |event|
      %tr
        %td= event.name
        %td= link_to 'Show', event
