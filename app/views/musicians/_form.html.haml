= form_for(@musician) do |f|
  - if @musician.errors.any?
    #error_explanation
      %h2
        = pluralize(@musician.errors.count, "error")
        prohibited this musician from being saved:
      %ul
        - @musician.errors.full_messages.each do |message|
          %li= message
  .actions
    = f.submit
